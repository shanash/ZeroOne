using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RecoveryTextNode : DamageTextBase
{
    public override void ShowText(double point, DAMAGE_TYPE dtype, Vector3 target_pos)
    {
        base.ShowText(point, dtype, target_pos);
    }
}
