namespace FluffyDuck.Util
{
    /// <summary>
    /// 이 값은 사용자 정의 값이다.
    /// BlackBoard에서 해당 이넘 타입을 사용하고 있음
    /// </summary>
    public enum BLACK_BOARD_KEY
    {
        NONE = 0,
        TEST_KEY_ID,
        GAME_TYPE,

        STORY_STAGE_ID,
        EDITOR_STAGE_ID,

    }
}

